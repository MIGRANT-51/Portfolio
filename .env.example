# ── Server ────────────────────────────────────────────────────────
PORT=3000

# ── MySQL ─────────────────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=your_mysql_password
DB_NAME=portfolio

# ── Admin Dashboard ───────────────────────────────────────────────
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_TOKEN=change_this_to_a_long_random_string

# ── Threat Intel APIs (free tiers) ───────────────────────────────
# virustotal.com → Account → API Key
VT_API_KEY=your_virustotal_key_here

# urlscan.io → Settings → API Key
URLSCAN_API_KEY=your_urlscan_key_here

# ── Contact Form Email (optional) ─────────────────────────────────
# Gmail App Password: myaccount.google.com/apppasswords
SMTP_USER=your_gmail@gmail.com
SMTP_PASS=your_16_char_app_password
CONTACT_EMAIL=nehalkalbande44@gmail.com
SITE_URL=http://localhost:3000
